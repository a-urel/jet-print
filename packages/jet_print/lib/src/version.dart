/// The declared version of the `jet_print` library.
///
/// Exposed through the public API for diagnostics and to establish the SemVer
/// baseline (Constitution Principle V, FR-012). It mirrors the `version:` field
/// in `pubspec.yaml`; keep the two in sync on every release.
const String jetPrintVersion = '0.1.0';
