// Web stub for the tolerant golden comparator setup.
// On Chrome, goldens are excluded via --exclude-tags golden, so no-op is fine.
// Imported by flutter_test_config.dart via conditional import.
Future<void> setupGoldenComparator() async {}
